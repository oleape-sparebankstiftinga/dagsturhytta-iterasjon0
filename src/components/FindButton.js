import React from 'react';

import Button from 'react-bootstrap/Button'

class Find extends React.Component {
    render() {
        return (
            <div>
                <Button variant="primary" className="find-button">Finn din hytte</Button>
            </div>
        );
    }
}

export default Find;
